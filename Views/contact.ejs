
    <% if(msg == 'add'){%>
        <div class="alert alert-success" role="alert">
          Contact succes add
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <% } else if( msg == 'edit'){%>
        <div class="alert alert-success" role="alert">
          Contact Succes edit
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
     <% }else if( msg == 'delete'){%>
         <div class="alert alert-success" role="alert">
            Contact Succes delete
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <% } %>


<div class="text-center">
    <% const name = "Rohot M.T. Silaban"%>
    <h1>Web server Node JS</h1>
    <p>welcome to My Page <%= name%></p>
</div>

    <h3 class="text-center">Tabel Contact List</h3>
   
        <a href="/contact/tambah" class="btn btn-success" class="ml-3"><i class="fa-solid fa-user-plus">Adds Contacts</i></a>
    
    
    <table class="table">
        <tr>
            <td>no</td>
            <td>Nama</td>
            <td>No.telpon</td>
            <td colspan="3">Action</td>
        </tr>
            <% if(data.length > 0 ){data.forEach(function(dat, i) { %>
                <form action=" " method="post">
                <tr>
                    <td ><%= i+1 %></td>
                    <td ><%= dat.name %></td>
                    <td ><%= dat.mobile%></td>
                    <div class="flex row justify-content-center">
                    <td ><a href="/contact/detail/<%= dat.name %>" class="btn btn-secondary"><i class="fa-solid fa-eye"></i></a></td>
                    <td ><a href="/contact/edit/<%= dat.name %>" class="btn btn-warning" ><i class="fa-solid fa-file-pen"></i></a></td>
                    <td ><a href="/contact/delete/<%= dat.name %>" class="btn btn-danger" onclick="alert('apakah anda yakin??')" ><i class="fa-solid fa-trash"></i></a></td>
                    </div>
                </tr>
            </form>
 
            <% })} else {  %>
                <tr>
                    <td colspan="5">data Not Found</td>
                </tr>
         <%   }; %>
    </table>

   


